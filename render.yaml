# Render Blueprint — deploy the API from GitHub in one click.
# Dashboard: use Streamlit Community Cloud (see README / deploy guide).
services:
  - type: web
    name: post-generator-api
    runtime: python
    plan: free
    region: frankfurt
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    domains:
      - api.kj-solution.com
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.9"
      - key: DEBUG
        value: "false"
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: APP_PUBLIC_URL
        value: https://api.kj-solution.com
      - key: DASHBOARD_URL
        value: https://kjsolutioncontentgenerator.streamlit.app
      - key: OPENAI_API_KEY
        sync: false
      - key: LINKEDIN_CLIENT_ID
        sync: false
      - key: LINKEDIN_CLIENT_SECRET
        sync: false
      - key: FACEBOOK_APP_ID
        sync: false
      - key: FACEBOOK_APP_SECRET
        sync: false
      - key: OAUTH_TOKEN_ENCRYPTION_KEY
        sync: false
      - key: PLATFORM_ADMIN_EMAIL
        sync: false
